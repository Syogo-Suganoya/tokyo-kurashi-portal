# システム基本設計書：くらしの道しるべ（生活お悩みルーティングポータル）

## 1. システム概要

生活の困りごと（防犯・ごみ分別・AED・こども食堂・税金の使い道 等）の多くには、既に優れた公式サービスが存在する。しかし住民はその存在を知らず、たとえ辿り着いても各サービスごとにUIも粒度も異なり、「結局どこを見ればいいのか」で迷子になる。

本サービスは、**住民の困りごとにその場で答えを返す**。困りごとを自然文で入力すると、AIが分野を判定し、東京都オープンデータから構築した**簡易版コンテンツで直接回答する**。そして簡易版で賄えない要望——対応していない自治体、申請や予約が必要な手続き、より詳細な情報——に対しては、**その限界を明示した上で最適な公式サービスへ送り出す**。

### 1.1 設計思想：「答えは自前で返す。足りない分だけ公式へ送る」

```
困りごと入力
    ↓
AIが分野判定 ＋ 必要な情報を抽出
    ↓
┌─────────────────────────────┐
│ 簡易版で回答（自前・オープンデータ）        │  ← ここが本体
│  例:「ペットボトル」→ 資源ごみ / 中身を洗って  │
└─────────────────────────────┘
    ↓ 賄えない場合は限界を明示して
┌─────────────────────────────┐
│「粗大ごみの申込みはこちら」→ 区の公式受付へ     │  ← 逃げではなく設計された引き継ぎ
└─────────────────────────────┘
```

この構造には3つの利点がある。

1. **「単なるリンク集」問題が構造的に消える。** ポータル自身が答えを持つため、リンクは主役ではなく出口になる。
2. **簡易版の弱点が正直さに転化する。** 「うちのデータは令和3年度時点です、最新は公式で」と明示することは、行政情報を扱うサービスとして減点ではなく加点になる。カバー範囲を偽らない設計を最初から組み込む。
3. **カバー範囲の穴をエスカレーションが吸収する。** 対応自治体が5区しかなくても、残りは公式へ送ればサービスとして成立する。段階的に広げられる設計になり、当日の実装量をコントロールできる。

### 1.2 前方針からの変更点
当初は「既存サービスへのAIルーティング＋隙間だけ自作」という構成だったが、**全テーマで簡易版を自前で持つ**構成に転換した。これに伴い、以前の決定「地域粒度は都全域に固定」は**テーマごとの判断に変更する**（§3のTier分類を参照）。理由は、調査の結果ほとんどの生活データが区市町村単位でしか公開されていないと判明したためで、都全域に固定すると簡易版がほとんど作れなくなる。代わりに、**未対応自治体はエスカレーションリンクで公式へ送る**ことで、カバー率の低さがサービスの破綻に直結しない構造にした。

---

## 2. 調査結果：テーマ別のデータ入手性（実測）

東京都オープンデータカタログAPIを実際に検索し、テーマごとに「簡易版を作れるか」を判定した。**簡易版が作れるかは、そのテーマに都全域データがあるか／自治体別データのスキーマが揃っているかで決まる。**

| Tier | 意味 | 該当テーマ | 実装コスト |
| :--- | :--- | :--- | :--- |
| **A** | 都全域の一括データが存在。CSV数本で完結 | 防犯 / 学び・体験 / バリアフリー | 低 |
| **B** | 自治体別だが**国の推奨データセット標準スキーマ**に準拠。列名の揺れを吸収すれば横断統合できる | ごみ分別 | 中 |
| **C** | 自治体別かつスキーマがバラバラ。名寄せに時間がかかる | 避難所 / AED / こども食堂 / 保育所 / 公衆トイレ | 高 |
| **D** | 有用な生データが無い、または既存サービスが十分に優秀で簡易版を作る意味が薄い | 税金 / 観光 / デジタル / 社会参加 / 気候変動 | — |

### 2.1 Tier A：都全域データあり（簡易版の主力）

| テーマ | データセット | 提供元 | 実測 |
| :--- | :--- | :--- | :--- |
| 防犯 | 区市町村の町丁別、罪種別及び手口別認知件数 | 警視庁 | **CSV 1本 / 5,266町丁 / 37列**。`市区町丁,総合計,凶悪犯計,粗暴犯計,侵入窃盗計,…` 都内全町丁を網羅 |
| 学び・体験 | 施設関連情報（公民館・社会教育会館・青少年施設・図書館・博物館・博物館類似施設・その他） | 東京都教育庁 | CSV 7本。**全て統一スキーマ**（`区市町村名,施設区分,施設名,所在地,緯度,経度,座標系,電話番号,備考`）。緯度経度つきで無加工で地図に載る |
| バリアフリー | 都営地下鉄4線の設備 / 都内飲食店 / 都立スポーツ施設 | 交通局・産業労働局・生活文化スポーツ局 | CSV 6本。**3局に分断**されており、住民から見れば1つの関心事なのに横断して探せない |

### 2.2 Tier B：ごみ分別（全数調査済み・30自治体をカバー可能）

カタログAPIからごみ分別関連のCSVリソース64本を収集し、**全件を実際にダウンロードしてスキーマを検証した**。結果、**30自治体・28,088品目が即座に取り込める**ことを確認した。当初想定（数自治体）を大きく上回る。

| 区分 | 自治体 |
| :--- | :--- |
| **23区（13）** | 千代田区・中央区・港区・新宿区・文京区・墨田区・江東区・品川区・杉並区・荒川区・板橋区・葛飾区・中野区 |
| **市町村（17）** | 八王子市・立川市・武蔵野市・三鷹市・府中市・調布市・小金井市・日野市・東村山市・国分寺市・狛江市・東大和市・東久留米市・多摩市・西東京市・羽村市・瑞穂町 |

品目数上位は瑞穂町2,437 / 立川市2,082 / 多摩市1,641 / 羽村市1,618 / 府中市1,542 / 調布市1,522。**市部の方が品目数が多い**傾向があり、23区は中野区942・文京区979を除くと数百件規模。

検証結果の全量は **[`gomi_taiou_jichitai.json`](gomi_taiou_jichitai.json)** に保存済み。自治体ごとのCSV URL・エンコーディング・列マッピング・分別区分の語彙一覧が入っており、**そのまま取り込みスクリプトの設定ファイルとして使える**。

#### スキーマは2系統に収束する
| 系統 | 列構成 | 該当 |
| :--- | :--- | :--- |
| **推奨データセット系** | `全国地方公共団体コード, ID, 地方公共団体名, ゴミの品目, (カナ), 分別区分, 注意点, 粗大ごみ回収料金` | 大多数。列名に `ゴミの分別方法_` 等のプレフィックスが付く変種あり |
| **50音索引系** | `50音, 品名, 種別, 料金, 説明` | 港区・文京区 |

いずれも `品目 / 分別区分 / 注意点 / 料金` に写像できる。**列名マッピング表を1枚持てば、自治体追加が設定への数行追記で済む。** 対応自治体を増やす作業がスケールする構造であり、当日は数自治体から始めて時間が許す限り広げられる。

#### 実装上の地雷（実測で判明）

**① 世田谷区のCSVは列とデータがズレている（要注意）**
ヘッダは9列 `…品目, 分別区分, 注意点, 料金種別, 料金…` だがデータ行は7列で、しかも**品目と分別区分の中身が入れ替わっている**。

```
hdr:  …, ごみの分別方法_品目, ごみの分別方法_分別区分, …
data: 131121, 131121S00001, 不燃ごみ, アイスピック, '', 無料, ''
                            ^^^^^^^^  ^^^^^^^^^^^^
                            ここが品目列       ここが分別区分列
```
列名を信じて機械的にパースすると「品目＝不燃ごみ／分別区分＝アイスピック」という壊れたデータが787件生成される。**取り込みスクリプトに「分別区分の異なり数が行数に近ければ異常と判定して落とす」バリデーションを必ず入れる。** 世田谷区は個別に列を入れ替える特例対応とするか、対応自治体から外す。

**② 台東区のCSVは404（カタログのリンク切れ）** — カタログには2本登録されているが両方とも取得できない。対応不可。

**③ 分別区分の語彙が自治体ごとに全く違う（設計上の最重要論点）**
30自治体を通じた分別区分ラベルの異なりは**997種類**。同じ「燃えるごみ」を指すのに以下が併存する。

| 表記 | 採用自治体数 |
| :--- | :--- |
| 燃やすごみ | 11 |
| 燃やせるごみ | 9 |
| 可燃ごみ | 6 |
| 燃やさないごみ / 燃やせないごみ / 不燃ごみ | それぞれ 8 / 8 / 6 |

→ **住民に表示するラベルは、その自治体の公式表記をそのまま出す。** 勝手に「可燃ごみ」等へ正規化すると、実際のごみ袋やカレンダーの表記と食い違い、住民を混乱させる。一方でアイコン表示や横断検索のために**内部の正規化キー（`burnable` / `non_burnable` / `oversized` / `recyclable` / `not_collected`）を別に持つ**二層構造にする。表示は公式表記、内部処理は正規化キー。この分離を型定義で強制する。

**④ その他**
- エンコーディングは UTF-8 BOM が19自治体、CP932 が11自治体。**判定ロジックが必須**（自治体ごとに固定してはいけない）
- カナ列があるのは9自治体のみ（立川市・多摩市・西東京市・板橋区・東久留米市・東村山市・日野市・三鷹市・荒川区）。港区・文京区の `50音` 列は**頭文字1文字**であり、カナ検索には使えない。表記ゆれ吸収は自前で実装する
- 粗大ごみ料金は28自治体にあり、「アイロン台 → 粗大ごみ・400円」まで答えられる
- 港区は1セルに複数区分が入る（例：アイロン → `粗大ごみ、不燃ごみ`）。分割して両方提示し、注意点（「最大辺が30cm以上のものは粗大ごみへ」）を併記する

### 2.3 Tier C・Dの扱い
- **Tier C** は簡易版を作らず、既存サービス（全国AEDマップ、むすびえこども食堂マップ、東京都防災アプリ等）へのエスカレーションのみとする。既存が十分に優秀で、名寄せコストに見合わない。
- **Tier D** も同様にリンクのみ。特に税金（シン・トセイ 都税収入見える化ダッシュボード）と観光（GO TOKYO、9言語＋AI翻訳121言語）は既存の完成度が高く、簡易版を作ることが劣化にしかならない。

**このTier判断そのものを画面とプレゼンに出す。** 「全テーマで無理に自作せず、データがあり横断に価値がある領域だけを自前で持ち、それ以外は優れた既存へ正直に送る」という判断は、技術的な誠実さとしてそのまま評価対象になる。

---

## 3. 簡易版コンテンツの詳細設計

全ての簡易版に**共通の画面契約**を持たせる。個別に作り込まず、同じ骨格に流し込むことで実装量を抑える。

```
┌────────────────────────────────┐
│ ① 答え（簡易版が返す結論）                    │
│ ② 出典・時点・カバー範囲（必ず表示）              │
│ ③ この簡易版でできないこと（明示）                │
│ ④ → 公式サービスへ（エスカレーションリンク）         │
└────────────────────────────────┘
```
②〜④を任意項目にせず**必須項目として型定義に入れる**。実装漏れがビルドエラーになり、「限界を書き忘れた画面」が存在し得なくなる。

### 3.1 ごみ分別 簡易版 `/gomi`（Tier B・デモの主役）
- **カバー範囲**：**30自治体・28,088品目**（§2.2、設定は [`gomi_taiou_jichitai.json`](gomi_taiou_jichitai.json)）
- **入力**：品目名（「ペットボトル」「アイロン台」）＋自治体
- **出力**：分別区分（自治体の公式表記）・注意点・（粗大ごみなら）回収料金
- **既存に勝る点**：区の公式チャットボットは1区ずつ別サイトで、UIも操作も自治体ごとに違う。ここでは**30自治体を同一UIで引ける**。引っ越しを挟んでも、実家と自宅を比べたくても、同じ画面で完結する。またAIチャットから直接答えが返るため、サイトを探して検索し直す必要がない
- **エスカレーション**：
  - 未対応自治体（渋谷区・世田谷区・台東区 等）→ その区市町村の公式ごみ分別ページへ
  - 粗大ごみと判定された品目 → 区の粗大ごみ受付センターへ（**申込みは簡易版では絶対にやらない**）
  - 品目が見つからない → 公式チャットボットへ
- **実装注意**：§2.2の地雷4点（世田谷区の列ズレ／台東区404／分別区分語彙の二層構造／エンコーディング判定）を必ず踏まえる。特に**分別区分は自治体の公式表記をそのまま表示する**こと

### 3.2 防犯 簡易版 `/bouhan`（Tier A）
- **入力**：町丁名または区市町村名
- **出力**：その町丁の罪種別内訳（凶悪犯・粗暴犯・侵入窃盗・非侵入窃盗等）と、区内・都内での相対的な位置づけ
- **既存に勝る点**：警視庁マップ・防犯マップは地図表示が主で、「引っ越し先候補2つを数字で比べる」ことがしにくい。町丁単位の数値比較に振り切る
- **エスカレーション**：地図で面的に見たい → 東京都防犯マップ／警視庁事件事故発生マップ
- **実装注意**：CP932。**認知件数は人口・昼間人口・繁華街の有無に強く影響される。単純な件数の大小で「危ない街」と断定する表示は絶対に避ける**（繁華街を持つ町丁が必ず上位に来る）。件数と併せて必ず注記を出す。行政データの誤読を誘発する見せ方は、審査でも実運用でも最大の減点要因になる

### 3.3 学びと体験の場 簡易版 `/manabi`（Tier A）
- **入力**：地図操作、または区市町村・施設区分での絞り込み
- **出力**：公民館・図書館・博物館・青少年施設等を**1レイヤーに統合した地図**＋一覧
- **既存に勝る点**：施設種別ごとにサイトが分かれている現状に対し、「近くで子どもと行ける場所」を種別をまたいで一望できる。都の若者向けサービスは相談窓口（若ナビα）しかなく、この導線は存在しない
- **エスカレーション**：開館時間・休館日・イベント予約 → 各施設の公式サイト
- **実装注意**：CP932 / 座標系JGD2011（地図表示では実用上WGS84と同視して問題ない）/ 施設名の先頭に `＊` `＊?` の記号混入あり、除去必要 / 都外施設が混在（台東区立少年自然の家＝長野県諏訪市）ため都内外フラグを持つ / **公開年次は令和3年度。時点を必ず画面に明示**

### 3.4 バリアフリー 簡易版 `/barrierfree`（Tier A）
- **入力**：条件チェックボックス（車椅子で移動できる／多目的トイレがある／段差2cm以下 等）
- **出力**：条件に合う駅・飲食店・スポーツ施設を**横断で**絞り込んだリスト
- **既存に勝る点**：交通局・産業労働局・生活文化スポーツ局が別々に公開しているデータを、住民の関心事である「車椅子で行ける場所」という1軸に束ねる。**本サービスのコンセプトを最も体現する画面**
- **エスカレーション**：都営地下鉄以外の鉄道 → 各鉄道会社のバリアフリー情報／実際の来店可否 → 店舗へ直接確認
- **実装注意**：
  - 飲食店・地下鉄データに**緯度経度が無い**（住所・駅名のみ）。MVPは地図ではなく**条件で絞れるリスト＋詳細カード**とする。自作①が地図、これがリストと形式を分けることで実装時間も分散する
  - 都営4線のみ。東京メトロ・JR・私鉄は含まないことを画面に明示
  - 駅名に路線コードが埋め込まれている（`（E-01）新宿西口`）ためパースが必要
  - 設備の有無は `〇` と空欄。**空欄が「無い」か「未調査」か判別できないため、`〇` のみを「あり」とし、それ以外は「情報なし」と表示する。**「なし」と断定すると、実際には設備があるのに当事者を排除してしまう

---

## 4. AI設計：分野判定から「ツール選択＋情報抽出」へ

簡易版が答えを返す構成になったため、AIの役割が変わる。以前は「どの外部サービスへ送るか」の分類タスクだったが、今は**どの簡易版をどんなパラメータで呼ぶか**を決めるタスクになる。Gemini API の **function calling** をそのまま使う。

### 4.1 各簡易版を1つのツールとして定義する
```jsonc
// 例
{ "name": "search_gomi",  "input": { "item": "ペットボトル", "municipality": "立川市" } }
{ "name": "search_bouhan", "input": { "area": "北沢2丁目" } }
{ "name": "search_barrierfree", "input": { "conditions": ["wheelchair_accessible", "accessible_toilet"] } }
{ "name": "no_tool" }  // 簡易版で扱えない分野 → キュレーションDBからリンクを返す
```

### 4.2 最重要の設計判断：LLMに答えとURLを生成させない
- **答えの本文はLLMが作らない。** ツールの戻り値（オープンデータそのもの）を画面に出す。LLMがやるのは「どのツールを呼ぶか」「入力文から品目名・地名を抜き出すか」だけ。**分別区分や犯罪件数をLLMが創作する経路が存在しない。**
- **URLもLLMが生成しない。** エスカレーション先はキュレーションDBに実在するIDでのみ参照し、サーバ側で検証する。存在しない行政URLの提示が構造的に起こり得ない。

これは実装詳細ではなく主張材料として扱う。提出フォーム3-2「何をAIにさせ、何を人が担保したか」への回答がそのままこれになる。**行政情報を扱うサービスとして、AIに答えさせない部分をどこに引いたかが設計の核心である。**

### 4.3 パラメータが足りないときの聞き返し
ごみ分別は自治体が特定できないと答えられない。`municipality` が抽出できなかった場合、推測で答えず**1問だけ聞き返す**（「どちらの区市町村にお住まいですか？」）。一度答えたらセッション中は保持し、以降のごみ分別質問では聞き返さない。

### 4.4 該当なし・障害時
- どの簡易版でも扱えない分野（Tier C・D）→ `no_tool` を返し、キュレーションDBから既存サービスのリンクカードを提示する。**この経路が従来の「リンク集」機能そのものであり、機能として残っている**
- LLM API障害時 → キーワード部分一致で簡易版へ直接ルーティングするフォールバックに切り替える。デモ当日にAPIが不調でも画面が死なない保険

---

## 5. エスカレーションの型（設計の要）

リンクを「答えられなかったときの逃げ」ではなく**設計された引き継ぎ**にするため、発火理由を4種類に類型化し、画面上でも理由を明示する。

| 型 | 発火条件 | 文言例 |
| :--- | :--- | :--- |
| **範囲外** | 未対応の自治体・路線・施設種別 | 「渋谷区はまだ対応していません。区の公式ページで調べられます」 |
| **鮮度** | データの公開時点が古い可能性 | 「令和3年度時点の情報です。最新は施設の公式サイトでご確認ください」 |
| **手続き** | 申請・予約・相談が必要 | 「粗大ごみの申込みは区の受付センターで行えます」 |
| **深掘り** | より詳細・地図・リアルタイム情報 | 「地図で周辺一帯を見たい方は防犯マップへ」 |

**「手続き」型は特に重要。** 簡易版は「知る」までを担い、申請・予約・相談といった**住民に責任が発生する行為は絶対に代行せず、必ず公式へ送る。** この線引きを設計段階で明文化しておくことは、社会実装の可能性を問う審査項目に対する直接の回答になる。

---

## 6. 画面遷移・UX設計

1. **トップ（困りごとチャット）**：自然文入力。下部に簡易版4本へのショートカットチップ。
2. **回答**：簡易版の結果を§3の共通契約（答え／出典・時点／できないこと／公式リンク）でカード表示。
3. **簡易版ページ**：`/gomi` `/bouhan` `/manabi` `/barrierfree`。チャットを介さず直接使える。
4. **カテゴリ一覧**：12テーマ × Tier分類のディレクトリビュー。どのテーマを自前で持ち、どれを既存に委ねたかが一目で分かる。

---

## 7. 技術スタック（実装難易度：★★★☆☆）

前方針より簡易版が4本に増えたため、難易度を★2→★3に引き上げる。

| レイヤー | 選定 | 備考 |
| :--- | :--- | :--- |
| フロントエンド | Next.js (App Router) + TypeScript + Tailwind CSS | 共通スタック |
| 地図 | MapLibre GL JS または Leaflet（`/manabi` のみ） | 地図の学習コストを1画面に閉じ込める |
| AI | Gemini API の function calling。簡易版4本＋`no_tool` をツール定義。APIキーは `GEMINI_API_KEY` | 独自MLモデル・ベクトルDB不要。**LLMは答えを生成せず、ツール選択とパラメータ抽出のみ** |
| データ取り込み | **ビルド時の一括変換スクリプト**（CSV取得 → CP932/UTF-8判定 → 列名マッピング → 正規化 → 静的JSON出力） | 実行時に都のサーバを叩かない。都側の障害・遅延がデモに影響しない。ごみ分別の自治体追加は**列名マッピング表への数行追記**で済む設計にする |
| キュレーションDB | リポジトリ内の TypeScript / JSON（**Supabaseは使わない**） | 数十件・書き込みなし。外部DBは過剰であり、無料枠の自動サスペンドで当日デモが落ちるリスクを排除できる（提出前チェックリスト参照）。型定義が効きIDの打ち間違いをビルド時に検出できる |
| ホスティング | Vercel | |

### 7.1 実装優先順位（時間切れ時の切り方）
1. **共通の画面契約と型定義**（§3の①〜④）— これが先にないと後から限界表示を足せない
2. **`/gomi`**（**立川市・中野区**の2自治体のみ）— デモで最も分かりやすく、エスカレーションの4類型が全部登場する。この2自治体を選ぶ理由は、①どちらも§2.2の30自治体に含まれ列ズレ等の地雷が無い、②市部と23区が1つずつ入る、③立川市が推奨データセット系・中野区が別系統（`ごみの品目 / 種別 / 説明`）であり、**列名マッピング表の価値が最初の2自治体の時点で実証される**、④立川市は品目2,082件・カナ列・粗大ごみ料金が揃っておりデモの弾が多い。**世田谷区は列ズレ（§2.2①）のため初期対応に含めない**
3. **AI tool use** — チャットから `/gomi` が呼べる状態
4. **`/bouhan`** — CSV 1本で完結、費用対効果が最も高い
5. **`/manabi`** — 地図が映える
6. **`/barrierfree`** — 時間が無ければリスト表示のみの簡易版に落とす

### 7.2 当日リスク
- **簡易版4本を全て中途半端にすると全滅する。** 1本ずつ「共通契約を満たした完成状態」にしてから次へ進む。3本で止まっても、Tier表で「作らない判断」を説明できる構造になっている
- ごみ分別の対応自治体が少ないことは**弱点ではなくエスカレーション設計の実演**として見せる。「渋谷区は未対応なので公式へ送ります」という挙動をデモに**あえて含める**

---

## 8. デモシナリオ（2分プレゼン想定）

1. **簡易版が答える**：「ペットボトルってどう捨てるの？」→ 区を1問聞き返し → **その場で「資源／中身をすすいでキャップとラベルを外す」と回答**。既存はチャットボットのあるサイトを探すところから始まる。
2. **エスカレーションが働く**：「アイロン台を捨てたい」→ 粗大ごみと判定 → **「申込みは区の受付センターへ」と公式リンク**。手続きは代行しないという線引きを見せる。続けて未対応自治体を聞き、範囲外エスカレーションも見せる。
3. **横断の価値**：「車椅子で行ける店や駅を知りたい」→ `/barrierfree`。3局にバラバラだったデータが1画面で絞り込めることを示す。
4. **締め**：Tier表スライド。「データがあり横断に価値がある領域だけ自前で持ち、既存が優れている領域には正直に送る」という判断を提示して終わる。

---

## 9. 政策的位置づけ

- **都知事杯ハッカソン2026公式テーマ**：「デジタル」テーマの課題「分野横断的な情報検索システム（知りたいことを起点に、分野を越えて行政情報、生活情報を検索できる仕組み）」に直接対応（出典: [odhackathon.metro.tokyo.lg.jp/issues/c10/clusters/](https://odhackathon.metro.tokyo.lg.jp/issues/c10/clusters/)）
- 簡易版という形で**「知りたいことを起点に、その場で答えが返る」**ところまで踏み込んでいる点が、検索システムの一段先にあたる
- `/barrierfree` は複数局に分散した情報の統合であり、行政のデータ連携課題そのものに触れている
- ごみ分別の**推奨データセット標準スキーマへの準拠が自治体横断サービスを可能にする**という実証は、オープンデータ政策への具体的なフィードバックになる
- 都知事選公約・チームみらい政策マニフェストとの直接の対応は未確認

---

## 10. 既存サービス一覧（エスカレーション先マスタ）

キュレーションDBの初期データ。Tier C・Dのテーマではこれが主たる回答になり、Tier A・Bでは簡易版の出口になる。

| # | テーマ | サービス | リンク先 | 役割 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 防災 | 東京都防災アプリ／防災ホームページ | https://www.bousai.metro.tokyo.lg.jp/1028747/ | 主回答（Tier C） |
| 2 | 防犯 | 東京都防犯ネットワーク「防犯マップ」 | https://www.bouhan.metro.tokyo.lg.jp/map/?m=1 | `/bouhan` の深掘り先 |
| 2 | 防犯 | 警視庁「事件事故発生マップ」 | https://www.keishicho.metro.tokyo.lg.jp/jiken_jiko/hassei/map_annai.html | `/bouhan` の深掘り先 |
| 3 | 健康 | 医療情報ネット「ナビイ」 | https://www.hokeniryo.metro.tokyo.lg.jp/iryo/iryo_hoken/medical_info/navii | 主回答（Tier C） |
| 3 | 健康 | 全国AEDマップ | https://www.qqzaidanmap.jp/ | 主回答（Tier C） |
| 3 | 健康 | まもろうよこころ（厚労省） | https://www.mhlw.go.jp/mamorouyokokoro/soudan/sonota/ | 主回答（手続き型） |
| 4 | 環境 | 渋谷区「ごみの品目別分別一覧（50音順）」 | https://www.city.shibuya.tokyo.jp/kurashi/gomi/kateigomi/gomi_hinmoku.html | `/gomi` の**範囲外**エスカレーション先（渋谷区はオープンデータ未公開＝未対応自治体） |
| 4 | 環境 | 中野区 ごみ分別チャットボット | https://www.city.tokyo-nakano.lg.jp/kurashi/gomi/chatbot.html | `/gomi` の**品目ヒットなし時**のエスカレーション先（中野区は対応自治体） |
| 5 | 気候変動 | 熱中症対策ポータル クールシェアスポットマップ | https://wbgt.metro.tokyo.lg.jp/effort/coolshare/ | 主回答（Tier D） |
| 6 | 生活 | 都税収入見える化ダッシュボード | https://shintosei.metro.tokyo.lg.jp/sainyudashboard/ | 主回答（Tier D・既存が優秀） |
| 6 | 生活 | Tokyo支援ナビ | https://www.support-navi.metro.tokyo.lg.jp/ | 汎用フォールバック先 |
| 7 | 社会参加 | ボラ市民ウェブ（東京ボランティア・市民活動センター） | https://www.tvac.or.jp/ | 主回答（Tier D） |
| 8 | 子ども・若者 | 東京都若者総合相談センター「若ナビα」 | https://www.wakanavi-tokyo.metro.tokyo.lg.jp/ | 主回答（手続き型） |
| 9 | 福祉 | こども食堂マップ（むすびえ×ガッコム） | https://kodomoshokudo.gaccom.jp/ | 主回答（Tier C） |
| 10 | デジタル | 東京都公式アプリ | https://www.tokyoapp.metro.tokyo.lg.jp/ | 主回答（Tier D） |
| 10 | デジタル | My TOKYO | https://www.my.metro.tokyo.lg.jp/ | 主回答（Tier D） |
| 11 | 交通 | 汎用乗換案内（Yahoo!路線情報／Googleマップ） | — | 一般的な移動はこれで足りるため自作しない |
| 12 | 観光 | GO TOKYO（9言語＋AI翻訳121言語） | https://www.gotokyo.org/jp/index.html | 主回答（Tier D・既存が優秀） |

---

## 11. 競合調査メモ

### 生活ガイド.com（https://www.seikatsu-guide.com/）
- 全国の市区町村の**行政データ＋住民口コミ**を集約したプラットフォーム。23区・全国市ごとに200項目以上の行政データを閲覧でき、2自治体間の比較機能を持つ
- カバー領域は補助金・住宅・子育て・医療・介護保険料・治安・待機児童・ふるさと納税・ハザードマップ・水道料金など広範。法人向けにレポート作成ツール・行政データ販売も展開
- **相違点**：粒度が「行政サービスの有無・数値」であり、**住民の具体的な困りごとに答えを返すものではない**。「ペットボトルをどう捨てるか」には答えられない。本サービスは自治体の比較ではなく、目の前の困りごとの解決に振り切る

### Tokyo支援ナビ（https://www.support-navi.metro.tokyo.lg.jp/）
- 東京都自身が運営する支援情報の横断検索サイトで、デジタルテーマ課題「分野横断的な情報検索システム」に最も近い既存サービス
- ただし**制度・窓口の検索**が主で、対象も都自身が実施するものが中心。区市町村独自のサービスや民間・NPOまでは横断していない
- 本サービスは**検索結果ではなく答えを返す**点、および区市町村・民間・NPOまで対象を広げる点で差別化する

### 総合判断
- 前方針で最大リスクだった「単なるリンク集に見える」問題は、簡易版が答えを持つ構成により**構造的に解消された**
- 新たな最大リスクは**簡易版の品質**。中途半端な自作は「公式より劣るものを作った」という評価になる。これを防ぐのが§3の共通画面契約（限界の明示）と§5のエスカレーション類型であり、**「劣っている部分を隠さず公式へ送る」ことで、簡易版が公式の劣化版ではなく入口として位置づけられる**

---

## 付録A. 未決事項

- [x] ~~`/gomi` の対応自治体をどこまで広げるか~~ → **調査完了。30自治体・28,088品目をカバー可能（§2.2 / [`gomi_taiou_jichitai.json`](gomi_taiou_jichitai.json)）**
- [ ] 世田谷区（列ズレ）を特例対応して31自治体にするか、対応外として範囲外エスカレーションに回すか判断
- [ ] 台東区のCSV 404 について、区の公式サイトに別URLの現行版がないか確認
- [ ] 分別区分の正規化キー（`burnable` / `non_burnable` / `oversized` / `recyclable` / `not_collected`）への997ラベルの対応表を作成
- [ ] `/barrierfree` に東京メトロのバリアフリー情報を追加できるか（東京メトロ公式のオープンデータ提供有無を調査）。追加できればカバー範囲が大きく広がる
- [ ] `/manabi` のデータ年次（令和3年度）について、より新しい版の公開有無を再確認
- [ ] `/bouhan` の件数表示における注記文言の確定（人口・繁華街要因による誤読を防ぐ表現）
- [ ] 提出フォーム 2-3 / 2-4 / 2-5 / 3-1 / 3-2 の310文字原稿作成（§1.1・§4.2・§5・§7から抽出可能）
