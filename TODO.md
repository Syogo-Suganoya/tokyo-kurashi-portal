# やること

提出期限：**2026-08-23**（作品提出フォーム）／今日：2026-08-20

上から順に、やらないと提出物として成立しないものを置いている。

---

## 1. 提出までに必ず必要

### 1-1. ~~デプロイして公開URLを確保する~~ 公開した

**https://tokyo-kurashi-portal.tokyo-odh-039.workers.dev/**

- [x] Cloudflare アカウント（有料プラン）で `npx wrangler login`
- [x] `.github/workflows/deploy.yml` の `CLOUDFLARE_ACCOUNT_ID` を実際のIDにした
- [x] APIトークンを作り、GitHub の `CLOUDFLARE_API_TOKEN` に登録した
- [x] `main` への push で CD が走る
- [x] 公開URLで4画面（`/gomi` `/bouhan` `/manabi` `/barrierfree`）とチャットが 200 で返る
- [ ] `/manabi` の地図がデプロイ先でも**点まで**描かれることを目で見る
- [ ] 共有カードの画像が出ることを確認する（`metadataBase` に公開URLを直書きしてある。**ここが古いと画像だけ静かに欠ける**）

> CD の `environment.url` に公開URLを書いたので、GitHub の Deployments と各コミットから辿れる。

### 1-2. AI の経路を検証する
- [x] 4つのツールが期待どおり選ばれることを workerd 上で確認した
  - `search_gomi` … 「アイロン台を捨てたい」→ **`municipality` を推測せず聞き返した**
  - `search_bouhan` … 「西新宿７丁目の治安が知りたい」→ 136件
  - `search_manabi` … 「中野区の図書館を教えて」
  - `search_barrierfree` … 「車椅子で入れるお店を探したい」→ 車椅子で入れる飲食店
- [x] `no_tool` の分野振り分けを確認した（`bousai` `zeikin` など）
- [x] **公開URLで6件を通した。**2件が壊れていたので直した（下記）
- [x] 直した2件を、再デプロイ後の公開URLで通し直した（どちらも直っている）
- [x] **モデルを実測で選び直した。**`llama-3.3-70b` → `@cf/qwen/qwen3-30b-a3b-fp8`
- [ ] 入れ替えたモデルを公開URLで通す

> **APIキーの設定作業は無くなった。** Workers AI をバインディングで呼ぶので、鍵を貼る場所が存在しない。
> 繋がらないときはキーワード判定に落ち、画面に「キーワード判定（理由）」と出る。
>
> 公開URLで見つけた壊れ方：
>
> | 入力 | 起きたこと | 直し方 |
> | :--- | :--- | :--- |
> | 「アイロン台を捨てたい」 | 品目が**「アイロン天」**になり0件 | 0件なら住民の文から実在する品目名を拾い直す（`itemFromText`） |
> | 「近くのAEDを探したい」 | 分野が `kikou` になり**涼める場所**を案内 | 分野コードの意味をツール定義に書いた（`kenko` と `kikou` を取り違えていた） |
>
> **モデルは入力の言葉を書き換える。** 地名（「西新宿７丁目」→「七丁部」）で分かっていたことが、
> 品目名でも起きていた。プロンプトで禁じても直らないので、受け取る側で吸収する。
>
> Workers AI 自体が時々「3044: Unknown internal error」を返す（連続10回中1回）。
> 1度だけ投げ直すようにした。それでも駄目ならキーワード判定に落ちる。

### 1-3. 提出フォームを埋める
- [ ] 提出フォームに転記する（各設問310文字以内・下書きは実測済み）
- [ ] **3-3 デモURL** をデプロイ後の実URLに差し替える
- [ ] 画面キャプチャ3枚を撮る
- [ ] プレゼン資料を作る（構成図は `docs/architecture.png`）
- [ ] 提出物に書いた数字（自治体数・件数・データ年次）が最新か確認する

> 設問の並びは実際のフォームに合わせてある。埋まっていないのは**デモURLとキャプチャだけ**で、
> どちらもデプロイ待ち。

### 1-4. デモの通し練習
- [ ] デモで使う入力を**公開URLで**通し直す（手元では確認済み）
- [ ] 読み上げて2分に収まるか計る

> 原稿は6枚・合計124秒で組んである。押したときに落とす文もスライド5に書いてある。

---

## 2. あると強い

### 2-1. ~~未対応3自治体の扱いを決める~~ 決めた（2026-08-17 に全件を再調査）
- [x] **世田谷区**：対応した。**特例処理は要らなかった。** 列名と中身が入れ替わっているだけで、どちらが品目かはデータ自身が示していた（異なり数 17 対 787）ので、設定で列を指し替えて済んだ。区のサイト側の787行版を使う（カタログ経由は165行）
- [x] **東村山市**：対応した。**案内先が死んでいたのではなく、こちらの名乗り方が弾かれていた。** 市のサイトのWAFが `Mozilla/5.0` だけの名乗りを403にしていた。一般的なボットの書式で名乗ると200。829品目
- [x] **台東区**：対応外を継続。カタログの2本（2024年版・2025年版）が**どちらも区のサイト側で404**。返るのはHTMLの404ページ
- [x] **渋谷区**：対応外を継続。品目ごとのオープンデータをそもそも公開していない

> **29自治体 → 31自治体・28,815品目**（都内62区市町村の半数超）。未対応は渋谷区・台東区の2区だけになった。
> どちらも公開側の状態が理由なので、「なぜ対応できていないか」を書いて公式へ送る形はそのまま残る。
>
> 再調査で見つけた別の不具合も直した。料金列に「500」のような金額と「無料」「有料」という**種別**が
> 混ざっており、種別に円を付けて**「無料円」と表示していた（10自治体）**。金額と種別を別々に持つようにした。

### 2-2. ~~チャットの聞き返しが密になっている~~ 対応済み
- [x] 選択式に変え、よく使う区市町村を最大3つまで端末に覚えるようにした（localStorage・サーバには送らない）
- [x] 覚えているときは聞き返さずに答える。`/gomi` にも近道を出す

### 2-3. ~~OGP と favicon~~ 対応済み
- [x] `openGraph` / `twitter` のメタデータを設定した。共有画像は1200×630のPNGをコミットしている
- [x] favicon を道しるべの標識に差し替えた（`src/app/icon.svg` ／ iOS用に `apple-icon.png`）

> 共有画像は `assets/opengraph-image.svg` を元に `node scripts/build-og-image.mjs` で作る。
> 手元で一度だけ実行してPNGをコミットする作りで、**ビルドには繋いでいない**。
> `next/og` の実行時生成は日本語フォントを別に用意しないと文字が全部豆腐になるため。

### 2-4. ~~自動テストがない~~ 書いた
- [x] 検索の純関数に21件のテストを書いた（`src/lib/search.test.ts`／`npm test`）
- [x] CIのビルドジョブでも走らせる

> 取り込み済みの実データをそのまま引く。部分一致・かな検索・3値・漢数字の丁目に加えて、
> **共通画面契約（出典・時点・できないこと・出口）が常に埋まっていること**も全件で見ている。
> 出口の `linkId` がキュレーションDBに実在するかもここで確かめる。

---

## 3. 時間があれば

- [x] ~~`/manabi` のデータ年次（令和3年度）について、より新しい版の公開有無を再確認する~~ 確認済み。**令和3年度版が最新**
- [x] ~~`/bouhan` に前年比を出す~~ 対応済み。あわせて**最新年を令和6年→令和7年に更新した**（R7.csv が2026-02-17に公開されていた）
- [x] ~~`/barrierfree` を地図でも見られるようにする~~ 対応済み。場所の種類で色分けし、座標を持つ2,000か所までを出す
- [x] ~~ごみ分別の分別区分アイコンを線画の図記号に揃える~~ 対応済み。絵文字をやめ、図記号の名前を型にした（`GlyphName`）
- [x] ~~`/manabi` の施設種別と `/barrierfree` の場所の種類の絵文字~~ 対応済み。図記号を12個足して線画に揃えた

> `/manabi` の年次はカタログAPIで再確認した。R4〜R7のURLは404で、カタログの資源リンクも全てR3を指している。
> `/bouhan` の前年比は**実数の差だけ**を出す。町丁の件数は1桁が多く、1件→3件が「+200%」になるため。
> 町丁の一覧は年で入れ替わるので（129町丁が突合できない）、**前年に同名の行が無い町丁には増減を出さない**。

- [x] ~~キーボード操作とスクリーンリーダーでの確認~~ 見て、3つ直した
  - **見出しが h1 → h3 に飛んでいた**（4画面すべて）。答えの見出しを h2 にして、見出しを辿ると最初に答えに着くようにした
  - **焦点のリングを自分で描いていなかった。** 既定任せだと配色によっては見えない
  - **フォームを飛ばして結果へ行けなかった。**`/barrierfree` は条件が8つ並ぶので、検索し直すたびに10回以上Tabを押すことになっていた。「結果へ移動」を足した
  - ラベルは全画面で入力と結び付いていた（`<label>` で包む形）。こちらは直す必要が無かった

---

## やらないと決めたこと

記録として残す。聞かれたときに答えられるようにしておく。

| 項目 | 理由 |
| :--- | :--- |
| 実行時にオープンデータを都度取得する | 公開CSVの更新は月〜年単位なのでリアルタイムにならず、**壊れたデータを止める門を失うだけ**。更新の検知だけ `check:data` に切り出した |
| 外部DB（Supabase等）を使う | 数十件・書き込みなしで過剰。無料枠の自動サスペンドで当日落ちるリスクを排除する |
| 乗換案内を自前で持つ | 民間のサービスが十分に優れている。駅のバリアフリー設備だけ自前で持つ |
| 避難所・AED・こども食堂などを自前で持つ | 自治体ごとにスキーマがバラバラで名寄せコストが見合わない。既存サービスが十分に優秀 |
| 申請・予約・申込みの代行 | 住民に責任が生じる行為は代行せず、必ず公式の窓口へ送る（設計の約束） |
