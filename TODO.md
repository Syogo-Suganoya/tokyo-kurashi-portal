# やること

提出期限：**2026-08-23**（作品提出フォーム）／今日：2026-08-17

上から順に、やらないと提出物として成立しないものを置いている。
実装状況は [docs/21_kurashi_michishirube_portal.md](docs/21_kurashi_michishirube_portal.md) §1.2 を参照。

---

## 1. 提出までに必ず必要

### 1-1. デプロイして公開URLを確保する
- [ ] Vercel にデプロイする
- [ ] 公開URLで4画面（`/gomi` `/bouhan` `/manabi` `/barrierfree`）とチャットが動くことを確認する
- [ ] `/manabi` の地図がデプロイ先でも出ることを確認する（ワーカーは `prebuild` で `public/` へ配置される）

> **これが最優先。** 動くURLが無いと提出できない。
> 生成JSONはコミット済みなので、ビルド時に行政のサーバへ取りに行かない。外部の都合でデプロイが失敗することはない。

### 1-2. Gemini API の経路を検証する
- [ ] `cp .env.example .env.local` して `GEMINI_API_KEY` を設定する（キーは各自で記入）
- [ ] 4つのツールが期待どおり選ばれるか確認する
  - `search_gomi` … 「アイロン台を捨てたい」で **`municipality` を推測せずに省略するか**
  - `search_bouhan` … 「西新宿７丁目の治安が知りたい」
  - `search_manabi` … 「中野区の図書館を教えて」
  - `search_barrierfree` … 「車椅子で入れるお店を探したい」
- [ ] 13の分野キーが正しく振り分けられるか確認する（`bousai` `kenko` `kikou` `shakai` `digital` `kotsu` など）
- [ ] Vercel の環境変数にも `GEMINI_API_KEY` を設定する

> キー未設定でもキーワード判定のフォールバックで全経路が動くことは確認済み。**画面が死ぬことはない**が、判定の質は落ちる。
> 現状は画面に「キーワード判定（APIキー未設定）」と表示される。

### 1-3. 提出フォームの原稿を仕上げる
- [ ] 実際の設問文を確認して [docs/22_teishutsu_form_draft.md](docs/22_teishutsu_form_draft.md) の力点を調整する
- [ ] 数字が最新か確認する（同ファイル末尾の「数字の出どころ」表と本文の両方）

> 原稿は5問ぶん書いてあるが、**設問文は仮置き**。提出フォームが応募者向けの非公開ページで文言を取得できなかった。
> 内容そのものは設問文に依存しないので、力点の調整だけで済むはず。

### 1-4. デモの通し練習
- [ ] [docs/21_kurashi_michishirube_portal.md](docs/21_kurashi_michishirube_portal.md) §8 のシナリオを公開URLで通す
- [ ] 2分に収まるか計る

---

## 2. あると強い

### 2-1. 未対応3自治体の扱いを決める
- [ ] **世田谷区**：列を入れ替える特例処理を書いて対応自治体に加えるか、今のまま「あえて対応していない」と説明し続けるか
- [ ] **台東区**：区の公式サイトに現行版のCSVが無いか探す（カタログのリンクは404）
- [ ] **東村山市**：公式ページに繋がらず案内先を確保できていない。別のURLを探す

> 今のままでも**説明として成立している**。とくに世田谷区は「壊れたデータを出さないために外した」と言えるので、
> 無理に増やすより現状のほうがデモでは強いかもしれない。

### 2-2. チャットの聞き返しが密になっている
- [ ] 自治体の選択肢が32個のボタンで並んでいる。よく使うものを先に出すか、選択式にするか検討する

> 対応自治体が2→29に増えた副作用。動作に問題はないが、初見だと圧が強い。

### 2-3. OGP と favicon
- [ ] `openGraph` のメタデータを設定する（SNSやSlackで共有したときに何のサービスか分かるように）
- [ ] favicon が Next.js の既定のままなので差し替える

### 2-4. 自動テストがない
- [ ] 検索の純関数（`searchGomi` / `searchBouhan` / `searchManabi` / `searchBarrierFree`）に最低限のテストを書く

> 取り込み側はビルド時の検証が手厚い（列ズレ・小計突合・疎通確認）が、**検索側は手で叩いて確認しているだけ**。
> 部分一致・かな検索・3値表示あたりは壊れても気づきにくい。

---

## 3. 時間があれば

- [ ] `/manabi` のデータ年次（令和3年度）について、より新しい版の公開有無を再確認する
- [ ] `/bouhan` に前年比を出す（令和5年分のCSVも公開されている。増減が分かると「引っ越し先を比べる」の価値が上がる）
- [ ] `/barrierfree` を地図でも見られるようにする（5,832か所は座標を持っている）
- [ ] ごみ分別の分別区分アイコンを、絵文字から `/manabi` と同じ線画の図記号に揃える
- [ ] キーボード操作とスクリーンリーダーでの確認（バリアフリーを扱うサービスとして、自分の画面が使えないのは筋が通らない）

---

## やらないと決めたこと

記録として残す。聞かれたときに答えられるようにしておく。

| 項目 | 理由 |
| :--- | :--- |
| 実行時にオープンデータを都度取得する | 公開CSVの更新は月〜年単位なのでリアルタイムにならず、**壊れたデータを止める門を失うだけ**。更新の検知だけ `check:data` に切り出した |
| 外部DB（Supabase等）を使う | 数十件・書き込みなしで過剰。無料枠の自動サスペンドで当日落ちるリスクを排除する |
| 乗換案内を自前で持つ | 民間のサービスが十分に優れている。駅のバリアフリー設備だけ自前で持つ |
| 避難所・AED・こども食堂などを自前で持つ | 自治体ごとにスキーマがバラバラで名寄せコストが見合わない。既存サービスが十分に優秀 |
| 申請・予約・申込みの代行 | 住民に責任が生じる行為は代行せず、必ず公式の窓口へ送る（設計の約束） |
