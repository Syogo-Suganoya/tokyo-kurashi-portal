# 困りごとチャットの分野判定に使う Gemini API キー。
# https://aistudio.google.com/apikey で取得し、.env.local にコピーして設定する。
#
# 未設定でもアプリは動く。その場合はキーワード一致のフォールバックに落ち、
# 画面には「キーワード判定（APIキー未設定）」と表示される（設計書 §4.4）。
GEMINI_API_KEY=

# 省略時は gemini-3.6-flash を使う
# GEMINI_MODEL=gemini-3.6-flash
